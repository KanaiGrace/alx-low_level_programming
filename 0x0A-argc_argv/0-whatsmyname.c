#include <stdio.h>

/**
 * main - peints program name follwed by new line
 * @argc: number of arguments
 * @argv: pointer
 * Return: 0
 */
int main(int argc, char *argv[])
{
if (argc == 1)
{
printf("%s\n", argv[0]);
}
return (0);
}
