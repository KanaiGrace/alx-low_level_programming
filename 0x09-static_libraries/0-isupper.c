#include <ctype.h>
#include "main.h"

/**
 * _isupper - indicates whether uppercase
 *
 * @c: holds value
 * Return: 0 or 1
 */
int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}
}
