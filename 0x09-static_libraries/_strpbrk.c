#include "main.h"
#include <string.h>

/**
 * _strpbrk - locates..
 * @s: points to adress of string
 * @accept: string accept
 * Return: pointer to s
 */
char *_strpbrk(char *s, char *accept)
{
return (strpbrk(s, accept));
}
