#include <stdio.h>
/**
 * product-prints number of arguments passed into it
 * @a:counts number of arguments
 * @b:pointer
 * Return:0
 */
int product(int a, int b)
{
product = a * b;
printf("%d\n", product);
return (0);
}
