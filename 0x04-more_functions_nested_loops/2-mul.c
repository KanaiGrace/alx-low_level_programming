#include "main.h"
#include <unistd.h>

/**
 * mul - performs multiplication
 * @a: holds value
 * @b: holds value
 * Return: 0
 */
int mul(int a, int b)
{
int product;
product = a * b;
return (product);
}
